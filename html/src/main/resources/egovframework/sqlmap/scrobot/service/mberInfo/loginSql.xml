<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="login">

	<select id="selectLoginDetail" parameterType="HashMap"
		resultType="String">
		/* login.selectLoginDetail*/
		<![CDATA[
		SELECT CASE WHEN COUNT(*)
		=
		0
		THEN 'N'
		ELSE
		'Y' END AS IdExistYn
		FROM T_SP_MBR_009
		WHERE BINARY(MBR_ID) = #{USER_ID}
		AND WHDWL_YN = 'N'
		]]>
	</select>

	<select id="selectPasswordDetail" resultType="String">
		/*login.selectPasswordDetail */
		SELECT
		PASSWORD FROM T_SP_MBR_009 WHERE
		BINARY(MBR_ID)=#{USER_ID}
	</select>

	<select id="selectNameDetail" resultType="String">
		/*login.selectNameDetail */

		SELECT
		MBR_NM FROM T_SP_MBR_009 WHERE
		BINARY(MBR_ID)=#{USER_ID}

	</select>

</mapper>