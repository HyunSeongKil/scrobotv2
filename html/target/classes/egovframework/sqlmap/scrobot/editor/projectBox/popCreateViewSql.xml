<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="popCreateView">

	<select id="importHTMLSource" parameterType="string" resultType="string">
		/* popCreateView.importHTMLSource */
		
		SELECT HTML_SOURC
		FROM T_RE_SIF_005
		WHERE SCRIN_ID = #{scrinId}		
		
	</select>

</mapper>